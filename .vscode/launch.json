{
    "version": "0.2.0",
    "configurations": [
        {
            "name": "Launch Project",
            "type": "cppvsdbg",
            "request": "launch",
            "program": "${config:godot_tools.editor_path_4}",
            "args": ["--debug"],
            "stopAtEntry": false,
            "cwd": "${workspaceFolder}/sample",
            "environment": [],
            "console": "integratedTerminal",
            "preLaunchTask": "build"
        }
    ]
}
